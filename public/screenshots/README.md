# Screenshots

Drop real UI captures into each project folder:

- `home-co-op/` — lists, calendar, task detail (± Telegram)
- `job-chaser/` — pipeline, agent command, favorites/archive (**redact personal data**)
- `trip-buddy/` — product UI when ready

Suggested filenames: `01-lists.png`, `02-calendar.png`.

Case-study pages auto-detect images here (and videos under `public/videos/<slug>/`). Placeholders show until files exist.
